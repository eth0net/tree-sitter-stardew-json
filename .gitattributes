# Corpus expectations and the generated parser are compared byte for byte, so a
# CRLF checkout on Windows would fail the regeneration check in CI.
* text=auto eol=lf

# Generated by `tree-sitter generate`. Collapsed in diffs, and kept out of the
# language statistics — parser.c is two orders of magnitude larger than the
# grammar it comes from.
src/parser.c linguist-generated
src/grammar.json linguist-generated
src/node-types.json linguist-generated
